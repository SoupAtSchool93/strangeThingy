import os, pygame, shutil, tkinter as tk #import-inator-inize
pygame.init()

#ze launch-inator window, and its stuffs
window_root = tk.Tk()
window_root.title("launcher inator")
window_root.geometry("300x200")
window_root.resizable(False, False)
window_root.configure(bg="#222")
#show the window
window_root.mainloop()